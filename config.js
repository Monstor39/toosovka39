// ============================================================
//  НАСТРОЙКИ МЕРОПРИЯТИЯ, БИЛЕТОВ, ЛИМИТОВ И КОДОВ
//  Меняй значения здесь.
// ============================================================

export const event = {
  title: "DUBAI PARTY",
  date: "сегодня, в субботу",
  time: "с 22:00",
  venue: "Правая Набережная 21, Калининград",
  telegram: "@TOOSOVKA39",
  telegramUrl: "https://t.me/TOOSOVKA39",
  instagram: "@imgolod",
  instagramUrl: "https://instagram.com/imgolod",
  reelUrl: "https://www.instagram.com/reel/DbUSGz5obZj/",
};

// Валюта ЮKassa — рубли
export const currency = "RUB";

// Билеты. limit — сколько всего можно взять этого типа (null — без ограничения).
// maxQty — сколько штук можно взять за один раз.
// codeFormat — формат уникального кода: letters букв + digits цифр.
// deposit: false — цена НЕ депозит (на баре эти деньги не тратятся).
// price: 0 — бесплатно: оплата не нужна, код выдаётся сразу после анкеты.
// Порядок в массиве = порядок карточек на сайте.
export const tickets = [
  {
    id: "free",
    name: "Бесплатный вход",
    price: 0,
    limit: null,
    maxQty: 6, // на себя и компанию
    deposit: false, // депозита на баре нет: бар оплачивается отдельно
    priceLabel: "Бесплатно",
    description:
      "Оставь телеграм или номер телефона — код на вход появится сразу на экране. Бар оплачиваешь на месте.",
    badge: "ВХОД СВОБОДНЫЙ",
    codeFormat: { letters: 1, digits: 3 }, // например A423
    soldOutText: "Запись закрыта",
  },
  {
    id: "vip",
    name: "Стол с депозитом",
    price: 10000,
    limit: 4,
    maxQty: 2,
    priceLabel: "10 000 ₽",
    description:
      "Свой стол на компанию до 4 человек. 10 000 ₽ — это депозит: всю сумму тратите за столом на баре.",
    badge: "СТОЛ",
    codeFormat: { letters: 2, digits: 2 }, // например AB42
    soldOutText: "Столы разобрали",
  },
];

export const ticketById = Object.fromEntries(tickets.map((t) => [t.id, t]));

// ============================================================
//  КОДЫ, ВЫДАННЫЕ ВРУЧНУЮ (владелец из бота: гостям, друзьям, за нал)
//  Это не запись с сайта — в статистике считаются отдельной строкой,
//  но на входе и на баре работают как обычный билет с депозитом.
//  Формат кода 2 буквы + 3 цифры (AB123) — не совпадает с форматами
//  билетов с сайта, поэтому такой код виден сразу.
// ============================================================
export const manualTicket = {
  id: "manual",
  name: "Гостевой код",
  codeFormat: { letters: 2, digits: 3 },
  deposits: [500, 1000, 1200, 2000, 3000, 5000], // быстрые кнопки суммы депозита
};

// Тест-режим: оплата стола имитируется (код выдаётся сразу).
// Станет false автоматически, когда впишешь ключи ЮKassa в .env.
// На бесплатный вход не влияет: там оплаты нет вообще.
export const TEST_MODE = !process.env.YOOKASSA_SHOP_ID || !process.env.YOOKASSA_SECRET_KEY;
